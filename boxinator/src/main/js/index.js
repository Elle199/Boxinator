import RouteLinks from './components/routes';
import './general'

const React = require('react');
const ReactDOM = require('react-dom');

/**
 * @author max.angman
 */

//Only used for the inital access point

ReactDOM.render(
	<RouteLinks />,
	document.getElementById('react')
)